<%- include('./includes/head.ejs') %>
<link rel="stylesheet" href="/css/home.css"> 

<body>
    <%- include('./includes/header.ejs') %>

    <main>
        <h1 id="homeTitle">Últimas operaciones :</h1>
        <section id="homeSection">
            <table id="homeTable">
                <% operations.forEach(element => { %>
                        <tr>
                            <td id="icon"> 
                                <% if (element.type == 'ingreso') { %>
                                    <i class="fa-solid fa-up-right"></i>
                                <% } else { %>
                                    <i class="fa-solid fa-down-right"></i>
                                <% } %> 
                            </td>
                            <td class="tableTds"><%= element.description %> </td>
                            <td class="tableTds" id="tableValue">$<%= element.value %> </td>
                            <td class="tableTds" id="tableDate"><%= element.date %> </td>
                        </tr>
                <% }) %>
            </table>
        </section>
    </main>
</body>
</html>